import { RangoDirective } from './rango.directive';

describe('RangoDirective', () => {
  it('should create an instance', () => {
    const directive = new RangoDirective();
    expect(directive).toBeTruthy();
  });
});
